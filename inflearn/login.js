window.addEventListener('load', () => {
    document.get
})

const login = () => {
    const memberId = document.getElementById("member-id");

    const result = alert(`${memberId.value}님 환영합니다~`);
    
}